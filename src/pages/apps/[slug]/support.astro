---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  const apps = await getCollection('apps', ({ data }) => data.published);
  return apps.map((entry) => ({
    params: { slug: entry.data.slug ?? entry.slug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const appSlug = entry.data.slug ?? entry.slug;
---

<BaseLayout title={`${entry.data.name} Support | Tess It`}>
  <main class="mx-auto max-w-3xl p-6">
    <a href={`/apps/${appSlug}/`}>← {entry.data.name}</a>
    <h1 class="my-8 text-3xl font-bold">{entry.data.name} Support</h1>
    <p>This is the support page placeholder for {entry.data.name}.</p>
    <p class="mt-4">
      Need help? Email <a href="mailto:support@tess-it.net">support@tess-it.net</a>.
    </p>
  </main>
</BaseLayout>
